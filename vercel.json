{
    "routes": [
        {
            "src": "/releases(.*)",
            "dest": "/api/releases.ts"
        },
        {
            "src": "/user(.*)",
            "dest": "/api/user.ts"
        }
    ]
}
