{
    "routes": [
        {
            "src": "/releases(.*)",
            "dest": "/api/releases.ts"
        },
        {
            "src": "/stats(.*)",
            "dest": "/api/stats.ts"
        }
    ]
}
